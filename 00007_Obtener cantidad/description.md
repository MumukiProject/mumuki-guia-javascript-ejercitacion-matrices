> Definí una función `obtenerCantidad` que tome como argumento un valor cualquiera `item` y un array 2d `items` y devuelva la cantidad de veces que `item` se encuentra dentro de `items`.
>
```javascript
obtenerCantidad('🍎', [
  ['🍎', '🍏', '🍌', '🍌'],
  ['🍌', '🍎'],
  ['🍎', '🍏', '🍌'],
  ['🍏', '🍌', '🍎', '🍌'],
])
4
```